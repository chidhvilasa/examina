# Development dependencies. Pinned to exact versions installed and
# verified in the phase that introduced them (Constitution Principle 8:
# dependencies added only when a phase actually needs them).

# Phase 1
pydantic==2.13.4
httpx==0.28.1
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.15.21
mypy==2.3.0

# Phase 4 — API
fastapi==0.139.2
uvicorn==0.51.0
slowapi==0.1.10
SQLAlchemy==2.0.51
psutil==7.2.2
python-multipart==0.0.32
types-psutil==7.2.2.20260518

# Phase 6 — Hardening (test-only: constructing synthetic stress-test fixtures)
Pillow==12.3.0
PyMuPDF==1.28.0
pip-audit==2.10.1
